# In order to find the the most similar cell lines. I think we should use Cluster algorithmn. There is no easy way to do that by Python.

# I was looking for something a little simpler than clustering, though that would be a good approach.